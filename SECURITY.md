# Security Policy

## Supported versions

当前项目处于早期开发阶段，只对最新发布版本提供安全修复。

## Reporting a vulnerability

请不要公开披露仍可被利用的漏洞。可以先通过 GitHub 维护者主页提供的联系方式私下报告，并包含受影响版本、复现条件、潜在影响和建议修复方式。请勿附带真实 API 密钥、登录 Cookie 或未经授权的数据。

## Security model

DramaLens 包含浏览器扩展、本地 HTTP 服务、音频解析、本地模型加载和可选的第三方 AI 请求，重点风险包括：

- API 密钥或 `.env` 意外泄露
- 恶意音频、模型或文档触发第三方解析器漏洞
- 本地服务被错误暴露到局域网或公网
- 扩展权限扩大后访问非预期页面
- AI 输出或素材文本中的 Prompt Injection 影响后续自动化
- 依赖包、模型下载和安装脚本的供应链风险

当前服务仅绑定 `127.0.0.1`；扩展只允许访问本地服务；仓库忽略 `.env`、模型、录音和生成文档。AI 输出被视为不可信内容，不应直接转化为 Shell、网络或文件系统操作。

## Maintainer checklist

- 发布前运行密钥扫描并检查 Git 历史
- 审查扩展权限和外部网络目标变化
- 固定或审查关键依赖版本
- 不执行 Issue、字幕或 AI 输出中提供的命令
- 对上传文件设置大小限制，并保持本地服务仅监听回环地址
