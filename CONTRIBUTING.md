# Contributing to DramaLens

感谢你愿意参与 DramaLens。

## 提交问题

提交 Issue 前请先搜索是否已有相同问题，并尽量提供：

- 操作系统、Chrome 与 Python 版本
- 扩展版本和本地服务日志
- 可复现步骤、预期结果与实际结果
- 已去除隐私信息的截图或最小测试样本

不要上传 API 密钥、登录凭据、未经授权的音视频、完整影视台词或包含个人信息的日志。

## Pull Request

1. Fork 仓库并从 `main` 创建功能分支
2. 保持改动聚焦，避免混入无关格式化
3. 为新增行为补充必要说明或测试
4. 确认没有提交 `.env`、模型、录音和生成文档
5. 在 PR 中说明改动原因、用户影响和验证方式

## 开发原则

- 本地优先，尽量减少不必要的数据上传
- 所有录音和外部 AI 调用都必须由用户明确触发
- 不加入绕过访问控制、隐藏接口抓取或自动下载受保护内容的功能
- AI 结果必须明确标注为需要人工复核
- 新增依赖应说明必要性和安全影响

参与本项目即表示你同意按照项目许可证贡献代码。
